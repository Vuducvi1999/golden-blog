

$(`#comment_content_<%=@comment.id%>`).text(`<%=@comment.content%>`)

$(`#comment_content_<%=@comment.id%>`).toggleClass('d-none')
.next().toggleClass('d-none').toggleClass('d-block')
.next().toggleClass('d-none').toggleClass('d-block')
